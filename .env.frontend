# Frontend Gateway Configuration

# Port to expose the nginx gateway on (default: 80)
# This serves the dashboard, driver app, and proxies API/WebSocket
GATEWAY_PORT=80

# Note: NEXT_PUBLIC_API_BASE is set to /api in docker-compose
# since all requests go through the nginx gateway
